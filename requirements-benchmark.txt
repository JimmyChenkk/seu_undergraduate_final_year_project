# Manual install list for benchmark reproduction.
# Keep installation manual so the project environment stays under user control.
numpy>=2.0
PyYAML>=6.0
torch>=2.2
scikit-learn>=1.5
matplotlib>=3.8
POT>=0.9
