method_name: source_only

backbone:
  name: fcn
  classifier_hidden_dim: 128
  dropout: 0.1

optimization:
  epochs: 1
  batch_size: 32
  learning_rate: 0.001
  weight_decay: 0.0

loss:
  name: cross_entropy

notes:
  - Conservative source-only probe with the default batch size.
  - Keeps the run short while checking whether moderate throughput changes GPU behavior.
