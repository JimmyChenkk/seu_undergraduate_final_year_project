method_name: source_only

backbone:
  name: fcn
  classifier_hidden_dim: 128
  dropout: 0.1

optimization:
  epochs: 10
  batch_size: 32
  learning_rate: 0.0001
  weight_decay: 0.0

loss:
  name: cross_entropy

notes:
  - Current in-repo source-only baseline shortened to 10 epochs for workflow comparison.
  - Keeps the existing hidden MLP classifier head and dropout settings unchanged.
