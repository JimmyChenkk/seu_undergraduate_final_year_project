method_name: source_only

backbone:
  name: fcn
  classifier_hidden_dim: 128
  dropout: 0.1

optimization:
  epochs: 1
  batch_size: 8
  learning_rate: 0.001
  weight_decay: 0.0

loss:
  name: cross_entropy

notes:
  - Minimal source-only probe config for conservative CUDA diagnosis.
  - Keeps the model unchanged while reducing peak batch load.
