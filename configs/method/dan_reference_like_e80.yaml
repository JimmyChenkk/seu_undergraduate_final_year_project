method_name: dan

backbone:
  name: fcn
  classifier_hidden_dim: 0
  dropout: 0.0

optimization:
  epochs: 80
  batch_size: 32
  learning_rate: 0.0001
  weight_decay: 0.0

loss:
  name: mkmmd
  adaptation_weight: 0.05

notes:
  - Reference-aligned DAN config for the long full-method round.
  - Keeps the current adaptation weight while reusing the improved source-side architecture.
