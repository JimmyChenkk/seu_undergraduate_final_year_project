method_name: source_only

backbone:
  name: fcn
  classifier_hidden_dim: 0
  dropout: 0.0

optimization:
  epochs: 20
  batch_size: 32
  learning_rate: 0.0001
  weight_decay: 0.0

loss:
  name: cross_entropy

notes:
  - Reference-aligned source-only config extended to 20 epochs.
  - Used to check whether the target-domain gain continues after the first strong architecture fix.
