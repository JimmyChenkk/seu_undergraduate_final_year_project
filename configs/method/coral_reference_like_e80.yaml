method_name: coral

backbone:
  name: fcn
  classifier_hidden_dim: 0
  dropout: 0.0

optimization:
  epochs: 80
  batch_size: 32
  learning_rate: 0.0001
  weight_decay: 0.0

loss:
  name: coral
  adaptation_weight: 0.5

notes:
  - Reference-aligned CORAL config for the long full-method round.
  - Shares the same encoder/head simplification that improved source-only performance.
