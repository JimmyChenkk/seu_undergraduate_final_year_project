method_name: source_only

backbone:
  name: fcn
  classifier_hidden_dim: 0
  dropout: 0.0

optimization:
  epochs: 10
  batch_size: 32
  learning_rate: 0.0001
  weight_decay: 0.0

loss:
  name: cross_entropy

notes:
  - Reference-aligned source-only config for workflow comparison.
  - Matches the public TEP baseline more closely by using no dropout and a direct linear classifier.
