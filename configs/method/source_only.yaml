method_name: source_only

backbone:
  name: fcn
  classifier_hidden_dim: 128
  dropout: 0.1

optimization:
  epochs: 10
  batch_size: 32
  learning_rate: 0.001
  weight_decay: 0.0

loss:
  name: cross_entropy

notes:
  - Minimal source-only baseline for the TEP benchmark.
  - Uses the same FCN-style encoder family as the public benchmark repository.
