experiment_name: debug_multisource
seed: 42
device: cpu
output_dir: runs

tracking:
  batch_root_name:

runtime:
  dry_run: true
  save_checkpoint: false
  save_analysis: false
  num_workers: 0

protocol_override:
  setting: multi_source
  source_domains:
    - mode2
    - mode3
    - mode4
    - mode5
    - mode6
  target_domain: mode1
  preferred_fold: Fold 1

notes:
  - Minimal multisource smoke-test config.
  - Uses five source modes to adapt to one target mode on CPU with dry_run enabled.
