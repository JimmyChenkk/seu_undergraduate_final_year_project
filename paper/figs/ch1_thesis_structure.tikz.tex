\begin{tikzpicture}[
    x=1cm,
    y=1cm,
    font=\small\fontspec{Times New Roman}\songti,
    >=Stealth,
    card/.style={
        draw,
        rounded corners=4.5pt,
        line width=0.45pt,
        fill=white,
        align=center,
        inner xsep=6pt,
        inner ysep=5pt,
        outer sep=0pt
    },
    bluecard/.style={card, draw=blue!45!black, fill=blue!4},
    cyancard/.style={card, draw=cyan!50!black, fill=cyan!5},
    orangecard/.style={card, draw=orange!65!black, fill=orange!6},
    purplecard/.style={card, draw=purple!45!black, fill=purple!5},
    greencard/.style={card, draw=green!45!black, fill=green!6},
    redcard/.style={card, draw=red!45!black, fill=red!5},
    panel/.style={
        draw=black!14,
        rounded corners=6pt,
        line width=0.35pt,
        fill=gray!1
    },
    flow/.style={
        line width=0.55pt,
        draw=black!62,
        line cap=round,
        line join=round
    },
    arrow/.style={
        flow,
        -{Stealth[length=3.6pt,width=4.0pt]},
        shorten >=0.4pt
    },
    routebar/.style={
        line width=0.42pt,
        draw=black!35,
        line cap=round
    },
    route/.style={
        font=\scriptsize\fontspec{Times New Roman}\songti,
        align=center,
        text=black!72,
        fill=gray!1,
        inner xsep=5pt,
        inner ysep=1pt
    }
]

% 灰色外框即最终 TikZ 盒子，中心线为 \CX。
\def\XMin{0.90}
\def\XMax{16.50}
\def\YMin{0.00}
\def\YMax{12.00}
\def\CX{8.70}
\def\LX{1.60}
\def\RX{15.80}
\def\VGap{0.52cm}
\def\YTopFold{8.55}
\def\YMethod{7.20}
\def\YMulti{4.55}
\def\YMergeFold{3.45}
\def\YSingleRoute{8.25}
\def\YMultiRoute{5.66}

% 主要节点
\node[bluecard, minimum width=3.50cm, minimum height=1.00cm] (ch1) at (\CX,11.25)
    {第一章\\[-1pt]绪论与研究路线};

\node[cyancard, minimum width=3.50cm, minimum height=1.00cm, below=\VGap of ch1] (ch2)
    {第二章\\[-1pt]基础理论与模型};

\node[orangecard, minimum width=3.70cm, minimum height=1.20cm] (ch3) at (6.20,\YMethod)
    {第三章\\[-1pt]TPU-DeepJDOT\\[-1pt]时序原型非平衡对齐};

\node[orangecard, minimum width=3.70cm, minimum height=1.20cm] (ch4) at (11.20,\YMethod)
    {第四章\\[-1pt]CBTPU-DeepJDOT\\[-1pt]多证据伪标签学习};

\node[purplecard, minimum width=8.70cm, minimum height=1.20cm] (ch5) at (\CX,\YMulti)
    {第五章\\[-1pt]CA-CCSR-WJDOT\\[-1pt]类条件源域可靠性加权};

\node[greencard, minimum width=3.50cm, minimum height=1.00cm] (ch6) at (\CX,2.35)
    {第六章\\[-1pt]实验验证与分析};

\node[redcard, minimum width=3.50cm, minimum height=1.00cm, below=\VGap of ch6] (ch7)
    {第七章\\[-1pt]总结与未来展望};

% 路由坐标
\coordinate (l3) at (\LX,\YMethod);
\coordinate (l5) at (\LX,\YMulti);

\coordinate (r4) at (\RX,\YMethod);
\coordinate (r5) at (\RX,\YMulti);

\coordinate (cTop) at (\CX,\YTopFold);
\coordinate (lTop) at (\LX,\YTopFold);

\coordinate (cMerge) at (\CX,\YMergeFold);
\coordinate (rMerge) at (\RX,\YMergeFold);

% 外框
\begin{scope}[on background layer]
\draw[panel] (\XMin,\YMin) rectangle (\XMax,\YMax);
\end{scope}

% 路线标签
\draw[routebar] (4.60,\YSingleRoute) -- (12.80,\YSingleRoute);
\node[route] at (\CX,\YSingleRoute) {单源方法递进路线};

\draw[routebar] (4.60,\YMultiRoute) -- (12.80,\YMultiRoute);
\node[route] at (\CX,\YMultiRoute) {多源选择性迁移路线};

% 连接关系
\draw[arrow] (ch1.south) -- (ch2.north);

\draw[flow]  (ch2.south) -- (cTop) -- (lTop) -- (l5);
\draw[arrow] (l3) -- (ch3.west);
\draw[arrow] (l5) -- (ch5.west);

\draw[arrow] (ch3.east) -- (ch4.west);

\draw[flow]  (ch4.east) -- (r4) -- (rMerge);
\draw[flow]  (ch5.east) -- (r5);
\draw[arrow] (rMerge) -- (cMerge) -- (ch6.north);

\draw[arrow] (ch6.south) -- (ch7.north);

\pgfresetboundingbox
\path[use as bounding box] (\XMin,\YMin) rectangle (\XMax,\YMax);

\end{tikzpicture}
