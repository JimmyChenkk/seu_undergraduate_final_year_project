\chapter{实验补充材料}

\section{附录说明}
本附录给出正文之外的补充材料，包括当前 88-run fixed-fold benchmark 的关键超参数、RCTA 配置摘要、复现实验命令以及完整结果表占位。所有内容均服务于本文正文聚焦的 benchmark 主实验与后续结果回填工作。

\section{基线与参考方法关键超参数}
表~\ref{tab:appendix-baseline-hparams} 汇总了当前 fixed-fold benchmark 中 7 种基线/参考方法采用的关键超参数。这些配置由
\path{configs/method/*.yaml}
与
\path{configs/experiment/benchmark_88_2clusters_11scenes_8methods_fixedfold.yaml}
共同决定。

\begin{table}[htbp]
\centering
\caption{fixed-fold benchmark 中基线与参考方法关键超参数}
\label{tab:appendix-baseline-hparams}
\scriptsize
\resizebox{\textwidth}{!}{%
\begin{tabular}{lccc p{8.0cm}}
\hline
方法 & epochs & batch size & 学习率 & 关键配置 \\
\hline
Source Only & 84 & 32 & $1\times 10^{-4}$ & 无显式对齐项，作为无自适应对照组 \\
Target Only & 112 & 32 & $1\times 10^{-4}$ & 使用目标域标签监督训练，仅作为参考上界 \\
CORAL & 96 & 32 & $1\times 10^{-4}$ & $\lambda_{coral}=0.55$，协方差均值对齐，warm-start 调度 \\
DAN & 84 & 32 & $1\times 10^{-4}$ & $\lambda_{mmd}=0.08$，5 组核尺度，constant 调度 \\
DANN & 84 & 32 & $1\times 10^{-4}$ & $\lambda_{adv}=0.40$，GRL warm-start，判别器隐层 256$\times$2 \\
CDAN & 96 & 32 & $1\times 10^{-4}$ & $\lambda_{cadv}=0.35$，randomized dim=512，熵条件化，MCC=0.03 \\
DeepJDOT & 120 & 64 & $1\times 10^{-4}$ & OT 权重 0.55，reg\_dist=0.08，reg\_cl=1.00，source-aware 选择指标 \\
\hline
\end{tabular}
}
\normalsize
\end{table}

\section{RCTA fixed-fold 配置摘要}
表~\ref{tab:appendix-rcta-hparams} 汇总了当前 benchmark 中 RCTA 采用的关键配置。该配置以 DANN 为基础对齐分支，同时融合 MCC 正则、可靠性门控、双原型记忆、一致性学习和课程化优化。

\begin{table}[htbp]
\centering
\caption{RCTA fixed-fold 配置摘要}
\label{tab:appendix-rcta-hparams}
\scriptsize
\begin{tabular}{l p{9.2cm}}
\hline
模块 & 配置 \\
\hline
训练轮数 & 112 \\
基础对齐分支 & DANN \\
MCC 正则 & $\lambda_{mcc}=0.04$，temperature=2.0 \\
教师网络 & EMA decay=0.992，teacher temperature=1.3 \\
可靠性权重 & cal\_conf=1.1，inv\_entropy=1.0，consistency=1.1，proto\_sim=1.2 \\
门控分数课程 & 0.34 $\rightarrow$ 0.56，持续 2200 steps \\
按类接纳比例课程 & 0.22 $\rightarrow$ 0.72，持续 2200 steps \\
可靠性分层 & reliable floor=0.66，semi-reliable floor=0.44 \\
伪标签损失 & 最大权重 0.22，warmup 900 steps，可靠性加权，power=1.4 \\
原型损失 & 最大权重 0.07，start step=800，warmup 1200 steps，separation weight=0.08 \\
一致性损失 & 最大权重 0.06，start step=700，warmup 1200 steps，gate-only=true，power=1.4 \\
辅助正则 & semi-reliable consistency=0.04，unreliable entropy=0.01 \\
原型记忆 & momentum=0.95，separation margin=0.20 \\
数据增强 & 弱增强：轻微抖动/缩放；强增强：更强抖动、缩放、时间屏蔽、通道 dropout \\
模型选择 & hybrid\_source\_eval\_confidence\_guard，带 overconfidence 和 domain gap 惩罚 \\
\hline
\end{tabular}
\normalsize
\end{table}

\section{benchmark 复现实验命令}
当前论文正文对应的主实验可直接通过如下命令运行：

\begin{quote}
\small
\ttfamily
scripts/run\_small\_scale\_round.sh \textbackslash\\
--experiment-config \textbackslash\\
configs/experiment/\textbackslash\\
benchmark\_88\_2clusters\_11scenes\_8methods\_fixedfold.yaml
\end{quote}

运行结束后，结果会在 \texttt{runs/} 下自动生成，并进一步汇总到 \texttt{comparison\_summary} 目录。推荐在结果回填时优先使用该汇总目录中的 JSON/Markdown 对照表和自动导出的图件，避免人工抄录时引入误差。

\section{完整结果表占位}
为了方便后续直接回填，本附录预留了比正文更完整的结果表结构。正文主要保留概览性表格，本附录则适合记录每个场景上的多指标结果。

\subsection{单源场景完整结果}
\begin{table}[htbp]
\centering
\caption{8 个单源场景的完整结果（占位）}
\label{tab:appendix-single-source-full}
\scriptsize
\resizebox{\textwidth}{!}{%
\begin{tabular}{lcccccc}
\hline
方法 & 场景 & 源域评估准确率 & 目标域准确率 & 目标域平衡准确率 & 选择 epoch & 备注 \\
\hline
\multicolumn{7}{c}{完整实验结果占位，待正式 benchmark 跑完后统一补充。} \\
\hline
\end{tabular}
}
\normalsize
\end{table}

\subsection{多源场景完整结果}
\begin{table}[htbp]
\centering
\caption{3 个多源场景的完整结果（占位）}
\label{tab:appendix-multi-source-full}
\scriptsize
\resizebox{\textwidth}{!}{%
\begin{tabular}{lcccccc}
\hline
方法 & 场景 & 各源域评估准确率 & 目标域准确率 & 目标域平衡准确率 & 选择 epoch & 备注 \\
\hline
\multicolumn{7}{c}{完整实验结果占位，待正式 benchmark 跑完后统一补充。} \\
\hline
\end{tabular}
}
\normalsize
\end{table}

\section{补充说明}
由于当前版本的重点是将论文结构与 benchmark 配置完全对齐，因此正文中的汇总图与代表性定性图均保留为占位实现。待正式结果产生后，可优先回填正文第七章中的总表与代表性图件，再将更细粒度指标补入本附录，从而形成“正文概览 + 附录明细”的完整呈现方式。
