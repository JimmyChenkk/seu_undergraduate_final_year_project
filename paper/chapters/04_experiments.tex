\chapter{基线方法与实验结果分析}

\section{基线方法说明}
本章围绕六种基线方法展开比较，包括 Source Only、CORAL、DAN、DANN、CDAN 和 DeepJDOT。所有方法共享第三章介绍的 FCN 骨干网络，因此方法差异主要体现在损失函数与对齐机制上。

\subsection{Source Only}
Source Only 仅依赖源域监督分类损失训练模型，不显式处理源域与目标域之间的分布偏移，其优化目标可写为
\begin{equation}
\mathcal{L}_{SO}=\mathcal{L}_{cls}^s.
\end{equation}
该方法可视为所有领域自适应方法的基础对照组。若某一领域自适应方法在目标域上不能显著优于 Source Only，则说明其对齐策略尚未真正缓解跨工况迁移带来的分布差异问题。

\subsection{分布对齐方法}
CORAL 通过约束源域与目标域特征协方差矩阵的一致性来减小二阶统计差异~\cite{sun2016deep}。设源域与目标域特征分别为 $F_s\in\mathbb{R}^{n_s\times d}$ 和 $F_t\in\mathbb{R}^{n_t\times d}$，对应均值向量与协方差矩阵为 $\mu_s,\mu_t,C_s,C_t$，则 CORAL 损失为
\begin{equation}
\mathcal{L}_{coral}=
\frac{\|C_s-C_t\|_F^2}{4d^2}
+\|\mu_s-\mu_t\|_2^2.
\end{equation}
总损失写为
\begin{equation}
\mathcal{L}_{CORAL}=\mathcal{L}_{cls}^s+\lambda_{coral}\mathcal{L}_{coral}.
\end{equation}

DAN 使用多核最大均值差异（MK-MMD）约束源域与目标域特征分布，在核空间中实现更细致的边缘分布对齐~\cite{long2015dan}。给定核函数族 $\{k_u\}_{u=1}^{U}$，其损失可表示为
\begin{equation}
\mathcal{L}_{mmd}=
\left\|
\frac{1}{n_s}\sum_{i=1}^{n_s}\varphi(f_i^s)-
\frac{1}{n_t}\sum_{j=1}^{n_t}\varphi(f_j^t)
\right\|_{\mathcal{H}}^2,
\end{equation}
实际实现中采用多个高斯核的加权和，于是总损失为
\begin{equation}
\mathcal{L}_{DAN}=\mathcal{L}_{cls}^s+\lambda_{mmd}\mathcal{L}_{mmd}.
\end{equation}
这两类方法的共同特点是训练目标较为直接，通常能在不引入额外判别器的情况下稳定收敛，因此适合作为基础分布对齐参照。

\subsection{对抗式方法}
DANN 在共享特征提取网络之后加入域判别器，并利用梯度反转层实现特征表示与域分类之间的对抗学习，从而逼近域不变特征~\cite{ganin2016dann}。设域标签 $d=1$ 表示源域，$d=0$ 表示目标域，域判别器为 $D(\cdot)$，则对抗损失可写为
\begin{equation}
\mathcal{L}_{adv}=
-\frac{1}{n_s}\sum_{i=1}^{n_s}\log D(f_i^s)
-\frac{1}{n_t}\sum_{j=1}^{n_t}\log(1-D(f_j^t)).
\end{equation}
在梯度反转层作用下，特征提取器朝着“混淆域判别器”的方向更新，因此总损失为
\begin{equation}
\mathcal{L}_{DANN}=\mathcal{L}_{cls}^s+\lambda_{adv}\mathcal{L}_{adv}.
\end{equation}

CDAN 在 DANN 基础上进一步将类别预测结果纳入条件化对齐过程，使域判别器感知类别结构信息~\cite{long2018conditional}。若记分类器输出概率为 $p(x)$，则 CDAN 对特征与类别预测进行联合映射：
\begin{equation}
g(x)=f(x)\otimes p(x),
\end{equation}
其中 $\otimes$ 表示多线性组合。域判别器不再直接作用于 $f(x)$，而是作用于 $g(x)$。当前实现中还支持随机映射版本与熵加权机制，并可附加最小类混淆损失（MCC）：
\begin{equation}
\mathcal{L}_{CDAN}=
\mathcal{L}_{cls}^s+\lambda_{cadv}\mathcal{L}_{cadv}+\lambda_{mcc}\mathcal{L}_{mcc}.
\end{equation}
这两类方法分别代表了全局对抗对齐和条件对抗对齐两种典型思路。

\subsection{最优传输方法}
DeepJDOT 通过联合建模特征空间距离与类别预测差异，构建源域和目标域样本之间的运输代价，在深度特征学习过程中同时优化分类与跨域匹配~\cite{damodaran2018deepjdot}。设 $\gamma$ 为源域与目标域样本间的运输矩阵，则其代价函数可写为
\begin{equation}
\mathcal{L}_{OT}=
\min_{\gamma\in\Pi(a,b)}
\sum_{i,j}\gamma_{ij}
\left(
\alpha\|f_i^s-f_j^t\|_2^2+
\beta\ell(y_i^s,p_j^t)
\right),
\end{equation}
其中 $\Pi(a,b)$ 表示满足边缘约束的运输计划集合，$\alpha$ 和 $\beta$ 分别控制特征距离代价与分类代价的权重。于是总损失写为
\begin{equation}
\mathcal{L}_{DeepJDOT}=\mathcal{L}_{cls}^s+\lambda_{ot}\mathcal{L}_{OT}.
\end{equation}
该方法能够提供样本级跨域关系约束，但也更容易受到批大小、代价尺度和计算开销的影响。

\section{实验范围与比较协议}
本章聚焦四个关键单源场景。具体而言，本文比较
\begin{equation*}
\mathrm{mode2}\rightarrow\mathrm{mode5},\quad
\mathrm{mode3}\rightarrow\mathrm{mode6},\quad
\mathrm{mode5}\rightarrow\mathrm{mode2},\quad
\mathrm{mode6}\rightarrow\mathrm{mode3}
\end{equation*}
四个迁移方向上的结果。所有结果均来自第三章给出的统一实验协议，即共享 FCN 骨干、批大小 32、学习率 $10^{-4}$、固定 Fold 1 划分以及一致的模型选择规则。除方法本身的对齐机制外，其余训练流程尽量保持一致，以便将性能差异尽量归因于适应机制本身。

在超参数层面，DAN 的 MK-MMD 权重为 0.05，DANN 的对抗权重为 0.5，CDAN 的条件对抗权重为 0.2，DeepJDOT 的 OT 权重为 1.0。该设置与论文附录中的配置表保持一致。

\section{实验结果与现象分析}
\subsection{四个关键单源场景上的总体结果}
表~\ref{tab:baseline-single-source} 预留了 6 种基线方法在四个关键单源场景上的目标域结果位置。考虑到当前版本需要移除与最终实验结果直接相关的数据，本表中的具体数值统一改为占位实现，待最终实验结果整理完成后再统一回填。

\begin{table}[htbp]
\centering
\caption{四个关键单源场景上的基线结果（占位）}
\label{tab:baseline-single-source}
\small
\resizebox{\textwidth}{!}{%
\begin{tabular}{lcccccc}
\hline
方法 & mode2$\rightarrow$mode5 & mode3$\rightarrow$mode6 & mode5$\rightarrow$mode2 & mode6$\rightarrow$mode3 & 平均准确率 & 平均平衡准确率 \\
\hline
\multicolumn{7}{c}{实验结果数据占位，待最终版本补充。} \\
\hline
\end{tabular}
}
\normalsize
\end{table}

在最终版本中，本节将基于该表比较不同基线方法在四个迁移方向上的整体表现，并从平均准确率、平均平衡准确率以及跨场景稳定性三个角度展开分析。当前先保留分析框架，避免在结果尚未最终确认前固化具体结论。

\subsection{分场景结果分析}
本小节保留四个关键迁移方向的逐场景分析入口。待结果回填后，将围绕迁移难度差异、方法收益边界以及方向不对称性展开讨论，以支撑后续改进方法的设计动机。

\paragraph{1. mode2$\rightarrow$mode5 场景}
此处预留对 mode2$\rightarrow$mode5 场景的结果解读，后续将结合最终回填的指标分析该方向上的分布偏移程度、基线收益结构以及误差模式。

\paragraph{2. mode3$\rightarrow$mode6 场景}
此处预留对 mode3$\rightarrow$mode6 场景的结果解读，后续将重点比较在相对易迁移方向上，显式对齐策略是否仍能带来稳定增益。

\paragraph{3. mode5$\rightarrow$mode2 场景}
此处预留对 mode5$\rightarrow$mode2 场景的结果解读，后续将重点观察困难迁移方向上的方法差异，以及互逆方向之间是否存在显著不对称性。

\paragraph{4. mode6$\rightarrow$mode3 场景}
此处预留对 mode6$\rightarrow$mode3 场景的结果解读，后续将重点分析在源域基线本身较强时，对齐压力控制是否会影响最终迁移收益。

\subsection{主要现象归纳}
待结果回填后，本节将基于总体比较与分场景现象，从以下三个角度总结基线实验的主要启示。

\begin{enumerate}
\item \textbf{跨工况分布偏移对目标域性能的影响程度。} 回填结果后，将重点评估不同迁移方向上的域差异如何影响基线方法表现。
\item \textbf{全局对齐策略的收益边界。} 回填结果后，将比较不同对齐机制在困难场景与相对易迁移场景中的适用性差异。
\item \textbf{类别结构利用与伪标签稳定性问题。} 回填结果后，将进一步分析仅依赖全局对齐时仍可能残留的类混叠与噪声传播现象。
\end{enumerate}

\section{对改进方法设计的启示}
基线实验的启示部分当前保留为占位分析。待最终结果回填后，本节将结合第四章的总体比较和分场景现象，进一步讨论后续方法设计需要重点回答的问题。现阶段仅保留与方法设计直接相关的分析框架：

\begin{enumerate}
\item 保留当前已经验证有效的强基线对齐能力，避免完全脱离已有经验重新设计；
\item 引入更直接的类条件结构约束，使跨域同类样本更容易聚合、异类样本更容易分离；
\item 对目标域伪标签进行可靠性控制，使无标签监督在训练早期不过度扰动分类边界，在训练后期又能逐步发挥作用。
\end{enumerate}

第五章提出的 RCTA 正是围绕这三点展开：以强基线对齐分支为底座，通过教师--学生时序一致性、可靠性门控和双原型记忆，把全局域对齐与类条件结构学习结合起来。

\section{本章小结}
本章在统一 FCN 骨干网络和统一实验协议下，对 Source Only、CORAL、DAN、DANN、CDAN 与 DeepJDOT 等基线方法建立了统一比较框架。与最终实验结果直接相关的表格和分析文字当前均已替换为占位实现，后续可围绕场景难度不对称、全局对齐收益边界以及类条件结构利用问题补入完整结论，从而为下一章的方法验证提供衔接。
